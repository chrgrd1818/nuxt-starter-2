# ============================================================================
# ROOT .gitignore — only artifacts/web/ is tracked
# ============================================================================

# Ignore everything by default
*

# Re-allow root .gitignore itself
!.gitignore

# Re-allow the artifacts directory path so web app files are reachable
!artifacts/
!artifacts/web/
!artifacts/web/**/*
